import { mutations } from './mutations';
import { resolvers } from './resolvers';
import { types } from './types';
import { subscriptions } from './subscriptions';

export const Message = { mutations, resolvers, types, subscriptions };
